Only place guidelines here after verifying the publisher, version, jurisdiction, and
reuse terms. Every PDF requires a same-name `.json` approval metadata file as described
in `medical_rag/README.md`.
